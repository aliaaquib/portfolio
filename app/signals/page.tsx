import { ContentList } from "@/components/content-list";

export default function SignalsPage() {
  return (
    <ContentList
      title="signals"
      category="signals"
      description="small movements, interface shifts, and patterns that seem worth keeping an eye on."
    />
  );
}
