import { ContentList } from "@/components/content-list";

export default function BuildsPage() {
  return (
    <ContentList
      title="builds"
      category="builds"
      description="experiments, setups, prototypes, and the systems i am actively assembling."
    />
  );
}
